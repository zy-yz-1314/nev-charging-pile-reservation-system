<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.powerqueue.mapper.ChargingPileMapper">

    <!--
      抢桩核心 SQL(并发防超卖)。
      条件 status='IDLE' AND version=#{version} 保证:
      高并发下只有一个请求能把桩从空闲改为已预约,其余更新影响行数为 0。
    -->
    <update id="grabPile">
        UPDATE charging_pile
        SET status  = 'RESERVED',
            version = version + 1
        WHERE id = #{pileId}
          AND status IN ('IDLE','RESERVED')
          AND version = #{version}
          AND deleted = 0
    </update>

    <!-- 状态流转(开始充电 / 释放为空闲等) -->
    <update id="updateStatus">
        UPDATE charging_pile
        SET status  = #{status},
            version = version + 1
        WHERE id = #{pileId}
          AND deleted = 0
    </update>

</mapper>
