package com.powerqueue.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.powerqueue.entity.ScoreWeightConfig;

/**
 * 打分权重配置 Mapper(L1)。
 */
public interface ScoreWeightConfigMapper extends BaseMapper<ScoreWeightConfig> {
}
